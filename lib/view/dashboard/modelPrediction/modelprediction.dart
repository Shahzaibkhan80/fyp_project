import 'package:flutter/material.dart';

class Modelprediction extends StatelessWidget {
  const Modelprediction({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("model prediction"),
    );
  }
}
